# À revoir

## Priorité actuelle

- [ ] Retenir les conventions : fichier `*_test.go` et fonction `TestXxx`.
- [ ] Dire quand utiliser `errors.Is` et quand utiliser `errors.As`.
- [ ] Distinguer une adresse, un pointeur et la valeur pointée avec `&` et `*`.

## Révision espacée

Après une notion, revenir dessus :

- le lendemain ;
- environ une semaine plus tard ;
- environ un mois plus tard.

## Questions rapides

1. Pourquoi `%w` est-il différent de `%v` dans `fmt.Errorf` ?
2. Pourquoi une struct passée à une fonction n’est-elle pas modifiée par défaut ?
3. Pourquoi une slice copiée par affectation peut-elle partager des données ?

Ne cocher un point que lorsque tu peux l’expliquer et retrouver un exemple dans le
dépôt sans relire la fiche.
